services:
  - type: web
    name: divideai-web
    runtime: static
    rootDir: divideai-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
